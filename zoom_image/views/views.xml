<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="zoom_image.list">
      <field name="name">zoom_image list</field>
      <field name="model">zoom_image.zoom_image</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="zoom_image.action_window">
      <field name="name">zoom_image window</field>
      <field name="res_model">zoom_image.zoom_image</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="zoom_image.action_server">
      <field name="name">zoom_image server</field>
      <field name="model_id" ref="model_zoom_image_zoom_image"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="zoom_image" id="zoom_image.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="zoom_image.menu_1" parent="zoom_image.menu_root"/>
    <menuitem name="Menu 2" id="zoom_image.menu_2" parent="zoom_image.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="zoom_image.menu_1_list" parent="zoom_image.menu_1"
              action="zoom_image.action_window"/>
    <menuitem name="Server to list" id="zoom_image" parent="zoom_image.menu_2"
              action="zoom_image.action_server"/>
    -->
  </data>
</odoo>